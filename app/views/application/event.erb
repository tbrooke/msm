<article class="type-system-geometric">
  <div class="main-body grid-items-lines">
    <h1> Events at Main Street</h1>
    <% @events.each do |article| %>
        <% if cell(:event, article) %>
            <%= cell(:event, article).call %>
        <% end %>
    <% end %>
  </div>
</article>
